---
title: Tutorials
description: Build the same non-trivial launch-readiness workflow in Clojure, Python, and TypeScript.
---

These tutorials build a launch-readiness assistant. Given a product-change
prompt, the workflow:

1. classifies the request as launch or incident,
2. runs product, engineering, security, and support reviewers,
3. synthesizes their notes into a go/no-go brief,
4. loops through critique before returning the final brief.

The examples are offline by default. They use deterministic mock responses so
you can inspect orchestration behavior before paying for model calls.

## Run the finished examples

```bash
clojure -M -e '(load-file "examples/clojure/launch.clj")'
python3 examples/python/launch.py
npx tsx examples/typescript/launch.ts
```

Expected final brief:

```text
Decision: launch to a staged enterprise beta cohort.
Top risks: SSO latency, unverified audit logs, and missing rollback messaging.
Next actions: confirm audit-log coverage, assign rollback owner, publish support docs, and monitor p95 auth latency during rollout.
```

## What the tutorials teach

| Page | Teaches |
| --- | --- |
| [Clojure](/docs/tutorials/clojure) | Direct API use, host reducer functions, and EDN workflow data. |
| [Python](/docs/tutorials/python) | Building workflow JSON from application dictionaries. |
| [TypeScript](/docs/tutorials/typescript) | Building the same workflow shape from typed application objects. |

## Agent configuration model

Each language builds agents from profile data before turning them into ordinary
karcarthy agent maps. The profile fields are application UI, not new workflow
resource types:

- role and mission define identity.
- operating context acts like a compact workspace bootstrap.
- tools are an adapter allowlist, not provisioned infrastructure.
- responsibilities describe the job to do.
- output contract makes downstream composition predictable.
- tone controls the user-facing experience.
- boundaries and self-checks keep the agent honest.

The product owns the configuration screen and templates. karcarthy only needs
the final agent instructions and workflow data.

## Workflow shape

The launch workflow uses the small functional vocabulary:

- `bind` chooses the launch path from a classifier result.
- `map` runs independent reviewers.
- `reduce` combines mapped reviewer output in Clojure.
- `pipe` turns review notes into a final draft.
- `iterate` gives the draft one or more critique rounds.

Python and TypeScript send JSON to `bin/karcarthy`, so host-language functions
cannot be sent as reducers. Those guides use a synthesizer agent in a `pipe`
after `map`, which is the portable cross-language shape.

<Cards>
  <Card title="Clojure" href="/docs/tutorials/clojure" />
  <Card title="Python" href="/docs/tutorials/python" />
  <Card title="TypeScript" href="/docs/tutorials/typescript" />
</Cards>
