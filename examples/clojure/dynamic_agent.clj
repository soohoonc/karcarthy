;; Visual, offline proof that a model Agent can submit, compile, and run a new
;; Agent form through karcarthy's built-in `agent` Tool.
(require '[karcarthy.demo.dynamic :as dynamic])

(dynamic/run-dynamic-demo!)
