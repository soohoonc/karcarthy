schema_version = "1.3"

[task]
name = "karcarthy/ledger"
description = "Repair transaction reconciliation by currency."
keywords = ["python", "maintenance", "finance"]

[metadata]
category = "software-engineering"

[agent]
timeout_sec = 120
user = "root"

[verifier]
timeout_sec = 60
user = "root"

[environment]
build_timeout_sec = 600
workdir = "/app"
