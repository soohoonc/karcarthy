schema_version = "1.3"

[task]
name = "karcarthy/target-vector"
description = "Write the exact TARGET value to answer.txt."

[agent]
timeout_sec = 120
user = "root"

[verifier]
timeout_sec = 60
user = "root"

[environment]
build_timeout_sec = 600
workdir = "/app"
