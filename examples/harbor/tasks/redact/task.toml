schema_version = "1.3"

[task]
name = "karcarthy/redact"
description = "Repair recursive redaction of sensitive structured data."
keywords = ["python", "maintenance", "security"]

[metadata]
category = "software-engineering"

[agent]
timeout_sec = 120
user = "root"

[verifier]
timeout_sec = 60
user = "root"

[environment]
build_timeout_sec = 600
workdir = "/app"
