schema_version = "1.3"

[task]
name = "karcarthy/retry"
description = "Repair exponential retry backoff with a cap."
keywords = ["python", "maintenance", "reliability"]

[metadata]
category = "software-engineering"

[agent]
timeout_sec = 120
user = "root"

[verifier]
timeout_sec = 60
user = "root"

[environment]
build_timeout_sec = 600
workdir = "/app"
