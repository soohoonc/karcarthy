schema_version = "1.3"

[task]
name = "karcarthy/scheduler"
description = "Investigate and repair a SQLite-backed delivery scheduler."
keywords = ["python", "debugging", "sqlite", "reliability"]

[metadata]
category = "software-engineering"
difficulty_explanation = "Requires repository inspection, test execution, transaction reasoning, priority ordering, and retry-state debugging."

[agent]
timeout_sec = 600
user = "root"

[verifier]
timeout_sec = 120
user = "root"

[environment]
build_timeout_sec = 900
workdir = "/app"
